'use client'

import { useEffect, useMemo, useState } from 'react'
import { ArrowDown, ArrowLeft, ArrowRight, Camera, Check, Clipboard, Gamepad2, Menu, Palette, Play, Sparkles, Trophy, Users, X, Zap } from 'lucide-react'

type Event = { id: string; name: string; group: string; teamSize: number | { min: number; max: number }; accent: string; image: string; description: string; prize: string; rules: string[]; icon: typeof Palette }
type Participant = { name: string; mobile: string; standard: 'PUC I' | 'PUC II' }
type Registration = { id: string; eventId: string; eventName: string; college: string; participants: Participant[]; createdAt: string }

const image = (id: string) => `https://images.unsplash.com/${id}?auto=format&fit=crop&w=900&q=82`
const EVENTS: Event[] = [
  { id: 'drawing', name: 'Drawing', group: 'Creative', teamSize: 1, accent: 'pink', image: image('photo-1547891654-e66ed7ebb968'), description: 'Turn an empty canvas into a world of your own.', prize: 'Prize details to be announced', rules: ['Bring your own materials.', 'Theme will be shared at the venue.'], icon: Palette },
  { id: 'rangoli', name: 'Rangoli', group: 'Creative', teamSize: 2, accent: 'orange', image: image('photo-1602523961358-f9f03dd557db'), description: 'Create colour, symmetry and wonder on the floor.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Materials and time limits will be shared soon.'], icon: Sparkles },
  { id: 'mehandi', name: 'Mehandi', group: 'Creative', teamSize: 2, accent: 'pink', image: image('photo-1594736797933-d0501ba2fe65'), description: 'Precision, patterns and a signature touch.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Bring your own cone and materials.'], icon: Sparkles },
  { id: 'dance-solo', name: 'Dance — Solo', group: 'Cultural', teamSize: 1, accent: 'violet', image: image('photo-1508700115892-45ecd05ae2ad'), description: 'Own the stage. Make every beat yours.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Performance duration will be shared soon.'], icon: Play },
  { id: 'group-dance', name: 'Group Dance', group: 'Cultural', teamSize: { min: 2, max: 12 }, accent: 'violet', image: image('photo-1535525153412-5a42439a210d'), description: 'Bring your crew and make the room move.', prize: 'Prize details to be announced', rules: ['Minimum 2 and maximum 12 participants.', 'Music and time limits will be shared soon.'], icon: Users },
  { id: 'singing', name: 'Singing', group: 'Cultural', teamSize: 1, accent: 'blue', image: image('photo-1524368535928-5b5e00ddc76b'), description: 'A voice, a stage and a moment to remember.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Bring backing track if required.'], icon: Play },
  { id: 'pubg-solo', name: 'PUBG Solo', group: 'Gaming', teamSize: 1, accent: 'blue', image: image('photo-1542751371-adc38448a05e'), description: 'Stay sharp. Survive longer. Take the win.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'pubg-squad', name: 'PUBG Squad', group: 'Gaming', teamSize: 4, accent: 'blue', image: image('photo-1593305841991-05c297ba4575'), description: 'Four players. One strategy. No second chances.', prize: 'Prize details to be announced', rules: ['Exactly 4 participants.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'freefire-solo', name: 'Free Fire Solo', group: 'Gaming', teamSize: 1, accent: 'orange', image: image('photo-1560253023-3ec5d502959f'), description: 'Fast decisions for players who never back down.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'freefire-squad', name: 'Free Fire Squad', group: 'Gaming', teamSize: 4, accent: 'orange', image: image('photo-1511512578047-dfb367046420'), description: 'Squad up and make your mark on the arena.', prize: 'Prize details to be announced', rules: ['Exactly 4 participants.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'quiz', name: 'Quiz', group: 'Challenge & Fun', teamSize: 2, accent: 'blue', image: image('photo-1434030216411-0b793f4b4173'), description: 'Fast minds, bold answers and one winning pair.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Quiz format will be shared soon.'], icon: Trophy },
  { id: 'treasure-hunt', name: 'Treasure Hunt', group: 'Challenge & Fun', teamSize: 2, accent: 'orange', image: image('photo-1527529482837-4698179dc6ce'), description: 'Decode clues. Chase the trail. Find the prize.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Be ready to move around campus.'], icon: ArrowDown },
  { id: 'tug-of-war', name: 'Tug of War', group: 'Challenge & Fun', teamSize: 8, accent: 'pink', image: image('photo-1518609878373-06d740f60d8b'), description: 'Eight on a rope. One team left standing.', prize: 'Prize details to be announced', rules: ['Exactly 8 participants.', 'Wear comfortable sportswear.'], icon: Users },
  { id: 'rodies', name: 'Rodies', group: 'Challenge & Fun', teamSize: 2, accent: 'pink', image: image('photo-1529156069898-49953e39b3ac'), description: 'Courage, chaos and challenges that test everything.', prize: 'Prize details to be announced', rules: ['2 participants per group.', 'Challenge format will be shared soon.'], icon: Zap },
  { id: 'photo-reels', name: 'Photo & Reels', group: 'Digital', teamSize: 2, accent: 'violet', image: image('photo-1516035069371-29a1b244cc32'), description: 'Capture the energy. Create the moment. Share the story.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Submission details will be shared soon.'], icon: Camera },
]
const groups = ['Creative', 'Cultural', 'Gaming', 'Challenge & Fun', 'Digital']
const teamLabel = (size: Event['teamSize']) => typeof size === 'number' ? `${size} participant${size === 1 ? '' : 's'}` : `${size.min}–${size.max} participants`
const initialParticipants = (event: Event): Participant[] => Array.from({ length: typeof event.teamSize === 'number' ? event.teamSize : event.teamSize.min }, () => ({ name: '', mobile: '', standard: 'PUC I' }))

export default function Page() {
  const [menuOpen, setMenuOpen] = useState(false); const [selected, setSelected] = useState<Event | null>(null); const [screen, setScreen] = useState<'home' | 'register' | 'success'>('home'); const [step, setStep] = useState(1); const [college, setCollege] = useState(''); const [participants, setParticipants] = useState<Participant[]>([]); const [registration, setRegistration] = useState<Registration | null>(null); const [registrations, setRegistrations] = useState<Registration[]>([]); const [copied, setCopied] = useState(false); const [errors, setErrors] = useState<string[]>([])
  useEffect(() => { try { setRegistrations(JSON.parse(localStorage.getItem('avishkar-registrations') || '[]')) } catch {} }, [])
  const grouped = useMemo(() => groups.map(group => ({ group, events: EVENTS.filter(event => event.group === group) })), [])
  const openRegister = (event: Event) => { setSelected(event); setParticipants(initialParticipants(event)); setCollege(''); setStep(1); setErrors([]); setScreen('register'); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const updateParticipant = (index: number, key: keyof Participant, value: string) => setParticipants(prev => prev.map((p, i) => i === index ? { ...p, [key]: value } : p))
  const validate = () => { const next: string[] = []; if (!college.trim()) next.push('Enter your college name.'); participants.forEach((p, i) => { if (!p.name.trim()) next.push(`Enter participant ${i + 1}'s full name.`); if (!/^\d{10}$/.test(p.mobile)) next.push(`Participant ${i + 1}'s mobile number must be 10 digits.`) }); setErrors(next); return !next.length }
  const submit = () => { if (!selected || !validate()) return; const entry: Registration = { id: `AVK-${Date.now().toString().slice(-6)}`, eventId: selected.id, eventName: selected.name, college: college.trim(), participants, createdAt: new Date().toISOString() }; const next = [...registrations, entry]; localStorage.setItem('avishkar-registrations', JSON.stringify(next)); setRegistrations(next); setRegistration(entry); setScreen('success'); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const copyId = async () => { if (!registration) return; await navigator.clipboard?.writeText(registration.id); setCopied(true); setTimeout(() => setCopied(false), 1800) }
  return <main className="site-shell"><header className="site-header"><a href="#home" className="wordmark" onClick={() => { setScreen('home'); setMenuOpen(false) }}><span>AVISHKAR</span><b>5.0</b></a><button className="menu-button" aria-label="Open menu" onClick={() => setMenuOpen(!menuOpen)}>{menuOpen ? <X /> : <Menu />}</button>{menuOpen && <nav className="mobile-menu"><a href="#events" onClick={() => setMenuOpen(false)}>Events</a><a href="#about" onClick={() => setMenuOpen(false)}>About</a></nav>}</header>
    {screen === 'home' && <><section className="hero" id="home"><div className="hero-copy"><p className="eyebrow">KLE BCA COLLEGE, NIPANI</p><div className="hero-mark"><span>AVISHKAR</span><b>5.0</b></div><p className="hero-tagline">Where talent meets competition.</p><p className="hero-description">An inter-college fest for PUC I &amp; PUC II students ready to show what they&apos;ve got.</p><div className="hero-meta"><span>DATE — TO BE ANNOUNCED</span><span>KLE BCA COLLEGE, NIPANI</span></div><div className="hero-actions"><button className="button button-primary" onClick={() => document.getElementById('events')?.scrollIntoView({ behavior: 'smooth' })}>Explore events <ArrowRight /></button><button className="button button-secondary" onClick={() => openRegister(EVENTS[0])}>Register now</button></div></div><HeroArt /><div className="scroll-cue"><ArrowDown /> Scroll to discover</div></section><section className="highlights" aria-label="Festival highlights">{[['FREE', 'REGISTRATION'], ['CASH', 'PRIZES'], ['15', 'EVENTS'], ['PUC I', '& PUC II']].map(([a, b]) => <div className="highlight" key={b}><strong>{a}</strong><span>{b}</span></div>)}</section><section className="events-section" id="events"><div className="section-heading"><p className="eyebrow">THE LINEUP</p><h2>Choose your <em>challenge.</em></h2><p>Tap an event to see details and register.</p></div>{grouped.map(({ group, events }) => <div className="event-group" key={group}><div className="group-title"><span>{group}</span><i>{String(events.length).padStart(2, '0')} EVENTS</i></div><div className="event-grid">{events.map((event, index) => <EventCard key={event.id} event={event} number={EVENTS.indexOf(event) + 1} onClick={() => setSelected(event)} />)}</div></div>)}</section><section className="about-section" id="about"><div><p className="eyebrow">THE AVISHKAR SPIRIT</p><h2>Bring your <em>boldest</em> self.</h2></div><p>One campus. Fifteen ways to stand out. AVISHKAR 5.0 is where ideas get loud, teams get competitive, and every participant gets their moment.</p></section><footer><div className="wordmark"><span>AVISHKAR</span><b>5.0</b></div><p>KLE BCA COLLEGE, NIPANI</p><span>Registration information will be announced soon.</span></footer></>}
    {screen === 'register' && selected && <RegistrationFlow event={selected} step={step} setStep={setStep} college={college} setCollege={setCollege} participants={participants} setParticipants={setParticipants} updateParticipant={updateParticipant} errors={errors} setErrors={setErrors} submit={submit} back={() => setScreen('home')} />}{screen === 'success' && registration && <SuccessScreen registration={registration} copied={copied} copyId={copyId} again={() => openRegister(EVENTS.find(e => e.id === registration.eventId) || EVENTS[0])} back={() => setScreen('home')} />}{selected && screen === 'home' && <EventPreview event={selected} close={() => setSelected(null)} register={() => openRegister(selected)} />}</main>
}
function HeroArt() { return <div className="hero-art" aria-hidden="true"><div className="hero-orbit orbit-one" /><div className="hero-orbit orbit-two" /><div className="hero-core"><span>A</span><span>5</span><span>0</span></div><div className="hero-spark spark-one" /><div className="hero-spark spark-two" /><div className="hero-spark spark-three" /></div> }
function EventCard({ event, number, onClick }: { event: Event; number: number; onClick: () => void }) { const Icon = event.icon; return <button className={`event-card accent-${event.accent}`} onClick={onClick}><div className="event-image"><img src={event.image} alt="" loading="lazy" /><div className="image-shade" /><Icon /><span className="card-number">{String(number).padStart(2, '0')}</span></div><div className="event-info"><span>{event.group}</span><h3>{event.name}</h3><small><Users /> {teamLabel(event.teamSize)}</small></div><ArrowRight className="card-arrow" /></button> }
function EventPreview({ event, close, register }: { event: Event; close: () => void; register: () => void }) { return <div className="overlay" role="presentation" onClick={close}><section className="sheet" role="dialog" aria-modal="true" aria-labelledby="preview-title" onClick={e => e.stopPropagation()}><button className="close-button" onClick={close} aria-label="Close"><X /></button><div className={`sheet-art accent-${event.accent}`}><img src={event.image} alt="" /><div className="image-shade" /><span>{event.group}</span></div><div className="sheet-content"><p className="eyebrow">{event.group}</p><h2 id="preview-title">{event.name}</h2><div className="team-pill"><Users /> {teamLabel(event.teamSize)}</div><p>{event.description}</p><h4>Prize</h4><p className="muted">{event.prize}</p><h4>Rules</h4><ul>{event.rules.map(rule => <li key={rule}>{rule}</li>)}</ul><button className="button button-primary wide" onClick={register}>Register now <ArrowRight /></button></div></section></div> }
function Progress({ step }: { step: number }) { return <div className="progress">{['College details', 'Participants', 'Review'].map((label, i) => <div className={step >= i + 1 ? 'active' : ''} key={label}><span>{i + 1}</span>{label}</div>)}</div> }
function RegistrationFlow({ event, step, setStep, college, setCollege, participants, updateParticipant, errors, setErrors, submit, back }: any) { const isTeam = typeof event.teamSize !== 'number'; const add = () => participants.length < event.teamSize.max && updateParticipant(participants.length, 'name', '') || null; return <section className="registration-page"><button className="back-link" onClick={back}><ArrowLeft /> Back to events</button><div className="registration-heading"><p className="eyebrow">REGISTRATION / {event.group.toUpperCase()}</p><h1>Join <em>{event.name}</em></h1><p>{teamLabel(event.teamSize)} · Registration is free.</p></div><Progress step={step} />{errors.length > 0 && <div className="error-box">{errors.map((error: string) => <span key={error}>{error}</span>)}</div>}{step === 1 && <div className="form-card"><h2>College details</h2><p>Tell us where your team is from.</p><label>College name<input value={college} onChange={(e: any) => { setCollege(e.target.value); setErrors([]) }} placeholder="Enter your college name" /></label><button className="button button-primary wide" onClick={() => college.trim() ? setStep(2) : setErrors(['Enter your college name.'])}>Continue <ArrowRight /></button></div>}{step === 2 && <div className="form-card"><div className="form-card-title"><div><h2>Participants</h2><p>Add everyone competing in this event.</p></div>{isTeam && <div className="stepper"><button onClick={() => participants.length > event.teamSize.min && setParticipants((p: Participant[]) => p.slice(0, -1))} disabled={participants.length <= event.teamSize.min}>−</button><strong>{participants.length}</strong><button onClick={() => participants.length < event.teamSize.max && setParticipants((p: Participant[]) => [...p, { name: '', mobile: '', standard: 'PUC I' }])} disabled={participants.length >= event.teamSize.max}>+</button></div>}</div>{participants.map((p: Participant, i: number) => <div className="participant-block" key={i}><h3>Participant {i + 1}</h3><label>Full name<input value={p.name} onChange={(e: any) => updateParticipant(i, 'name', e.target.value)} placeholder="Enter full name" /></label><label>Mobile number<input inputMode="numeric" maxLength={10} value={p.mobile} onChange={(e: any) => updateParticipant(i, 'mobile', e.target.value.replace(/\D/g, ''))} placeholder="10-digit mobile number" /></label><label>Standard<select value={p.standard} onChange={(e: any) => updateParticipant(i, 'standard', e.target.value)}><option>PUC I</option><option>PUC II</option></select></label></div>)}<div className="form-actions"><button className="button button-secondary" onClick={() => setStep(1)}>Back</button><button className="button button-primary" onClick={() => { setErrors([]); setStep(3) }}>Review <ArrowRight /></button></div></div>}{step === 3 && <div className="form-card"><h2>Review your registration</h2><div className="review-row"><span>Event</span><strong>{event.name}</strong></div><div className="review-row"><span>College</span><strong>{college}</strong></div><div className="review-participants"><span>Participants</span>{participants.map((p: Participant, i: number) => <div key={i}><strong>{p.name || `Participant ${i + 1}`}</strong><small>{p.standard} · {p.mobile || 'Mobile pending'}</small></div>)}</div><div className="form-actions"><button className="button button-secondary" onClick={() => setStep(2)}>Edit details</button><button className="button button-primary" onClick={submit}>Submit registration <Check /></button></div></div>}</section> }
function SuccessScreen({ registration, copied, copyId, again, back }: any) { return <section className="success-page"><div className="success-mark"><Check /></div><p className="eyebrow">REGISTRATION CONFIRMED</p><h1>You&apos;re <em>in.</em></h1><p className="success-lede">Bring your best. We&apos;ll see you at AVISHKAR 5.0.</p><div className="ticket"><span>EVENT</span><strong>{registration.eventName}</strong><span>REGISTRATION ID</span><b>{registration.id}</b><button onClick={copyId}>{copied ? <Check /> : <Clipboard />} {copied ? 'Copied' : 'Save registration ID'}</button></div><div className="success-actions"><button className="button button-primary" onClick={again}>Register another event <ArrowRight /></button><button className="button button-secondary" onClick={back}>Back to events</button></div></section> }
