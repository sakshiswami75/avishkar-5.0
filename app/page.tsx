'use client'

import { useEffect, useMemo, useState } from 'react'
import {
  ArrowDown, ArrowLeft, ArrowRight, Check, ChevronDown, Clipboard, Download,
  ExternalLink, Gamepad2, Camera, Menu, Palette, Play, RefreshCw, Sparkles,
  Trophy, Users, X, Zap,
} from 'lucide-react'

type Event = {
  id: string
  name: string
  group: string
  teamSize: number | { min: number; max: number }
  accent: string
  description: string
  prize: string
  rules: string[]
  icon: typeof Palette
}
type Participant = { name: string; mobile: string; standard: 'PUC I' | 'PUC II' }
type Registration = { id: string; eventId: string; eventName: string; college: string; participants: Participant[]; createdAt: string }

const EVENTS: Event[] = [
  { id: 'drawing', name: 'Drawing', group: 'Creative', teamSize: 1, accent: 'pink', description: 'Turn an empty canvas into a world of your own.', prize: 'Prize details to be announced', rules: ['Bring your own materials.', 'Theme will be shared at the venue.'], icon: Palette },
  { id: 'rangoli', name: 'Rangoli', group: 'Creative', teamSize: 2, accent: 'orange', description: 'Create colour, symmetry and wonder on the floor.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Materials and time limits will be shared soon.'], icon: Sparkles },
  { id: 'mehandi', name: 'Mehandi', group: 'Creative', teamSize: 2, accent: 'pink', description: 'Precision, patterns and a signature touch.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Bring your own cone and materials.'], icon: Sparkles },
  { id: 'dance-solo', name: 'Dance — Solo', group: 'Cultural', teamSize: 1, accent: 'violet', description: 'Own the stage. Make every beat yours.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Performance duration will be shared soon.'], icon: Play },
  { id: 'group-dance', name: 'Group Dance', group: 'Cultural', teamSize: { min: 2, max: 12 }, accent: 'violet', description: 'Bring your crew and make the room move.', prize: 'Prize details to be announced', rules: ['Minimum 2 and maximum 12 participants.', 'Music and time limits will be shared soon.'], icon: Users },
  { id: 'singing', name: 'Singing', group: 'Cultural', teamSize: 1, accent: 'blue', description: 'A voice, a stage and a moment to remember.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Bring backing track if required.'], icon: Play },
  { id: 'pubg-solo', name: 'PUBG Solo', group: 'Gaming', teamSize: 1, accent: 'blue', description: 'Stay sharp. Survive longer. Take the win.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'pubg-squad', name: 'PUBG Squad', group: 'Gaming', teamSize: 4, accent: 'blue', description: 'Four players. One strategy. No second chances.', prize: 'Prize details to be announced', rules: ['Exactly 4 participants.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'freefire-solo', name: 'Free Fire Solo', group: 'Gaming', teamSize: 1, accent: 'orange', description: 'Fast decisions for players who never back down.', prize: 'Prize details to be announced', rules: ['One participant per entry.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'freefire-squad', name: 'Free Fire Squad', group: 'Gaming', teamSize: 4, accent: 'orange', description: 'Squad up and make your mark on the arena.', prize: 'Prize details to be announced', rules: ['Exactly 4 participants.', 'Match format will be shared soon.'], icon: Gamepad2 },
  { id: 'quiz', name: 'Quiz', group: 'Challenge & Fun', teamSize: 2, accent: 'blue', description: 'Fast minds, bold answers and one winning pair.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Quiz format will be shared soon.'], icon: Trophy },
  { id: 'treasure-hunt', name: 'Treasure Hunt', group: 'Challenge & Fun', teamSize: 2, accent: 'orange', description: 'Decode clues. Chase the trail. Find the prize.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Be ready to move around campus.'], icon: ArrowDown },
  { id: 'tug-of-war', name: 'Tug of War', group: 'Challenge & Fun', teamSize: 8, accent: 'pink', description: 'Eight on a rope. One team left standing.', prize: 'Prize details to be announced', rules: ['Exactly 8 participants.', 'Wear comfortable sportswear.'], icon: Users },
  { id: 'rodies', name: 'Rodies', group: 'Challenge & Fun', teamSize: 2, accent: 'pink', description: 'Courage, chaos and challenges that test everything.', prize: 'Prize details to be announced', rules: ['2 participants per group.', 'Challenge format will be shared soon.'], icon: Zap },
  { id: 'photo-reels', name: 'Photo & Reels', group: 'Digital', teamSize: 2, accent: 'violet', description: 'Capture the energy. Create the moment. Share the story.', prize: 'Prize details to be announced', rules: ['Teams of two participants.', 'Submission details will be shared soon.'], icon: Camera },
]

const groups = ['Creative', 'Cultural', 'Gaming', 'Challenge & Fun', 'Digital']
const teamLabel = (size: Event['teamSize']) => typeof size === 'number' ? `${size} participant${size === 1 ? '' : 's'}` : `${size.min}–${size.max} participants`
const initialParticipants = (event: Event): Participant[] => {
  const count = typeof event.teamSize === 'number' ? event.teamSize : event.teamSize.min
  return Array.from({ length: count }, () => ({ name: '', mobile: '', standard: 'PUC I' as const }))
}

function HeroArt() { return <div className="hero-art" aria-hidden="true"><div className="hero-orbit orbit-one" /><div className="hero-orbit orbit-two" /><div className="hero-core"><span>A</span><span>5</span><span>0</span></div><div className="hero-spark spark-one" /><div className="hero-spark spark-two" /><div className="hero-spark spark-three" /></div> }

export default function Page() {
  const [menuOpen, setMenuOpen] = useState(false)
  const [selected, setSelected] = useState<Event | null>(null)
  const [screen, setScreen] = useState<'home' | 'register' | 'success' | 'admin'>('home')
  const [step, setStep] = useState(1)
  const [college, setCollege] = useState('')
  const [participants, setParticipants] = useState<Participant[]>([])
  const [registration, setRegistration] = useState<Registration | null>(null)
  const [registrations, setRegistrations] = useState<Registration[]>([])
  const [copied, setCopied] = useState(false)
  const [errors, setErrors] = useState<string[]>([])

  useEffect(() => { try { setRegistrations(JSON.parse(localStorage.getItem('avishkar-registrations') || '[]')) } catch {} }, [])
  const grouped = useMemo(() => groups.map(group => ({ group, events: EVENTS.filter(event => event.group === group) })), [])
  const openRegister = (event: Event) => { setSelected(event); setParticipants(initialParticipants(event)); setCollege(''); setStep(1); setErrors([]); setScreen('register'); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const updateParticipant = (index: number, key: keyof Participant, value: string) => setParticipants(prev => prev.map((p, i) => i === index ? { ...p, [key]: value } : p))
  const addParticipant = () => selected && typeof selected.teamSize !== 'number' && participants.length < selected.teamSize.max && setParticipants(prev => [...prev, { name: '', mobile: '', standard: 'PUC I' }])
  const removeParticipant = () => selected && typeof selected.teamSize !== 'number' && participants.length > selected.teamSize.min && setParticipants(prev => prev.slice(0, -1))
  const validate = () => { const next: string[] = []; if (!college.trim()) next.push('Enter your college name.'); participants.forEach((p, i) => { if (!p.name.trim()) next.push(`Enter participant ${i + 1}'s full name.`); if (!/^\d{10}$/.test(p.mobile)) next.push(`Participant ${i + 1}'s mobile number must be 10 digits.`) }); setErrors(next); return next.length === 0 }
  const submit = () => { if (!selected || !validate()) return; const entry: Registration = { id: `AVK-${Date.now().toString().slice(-6)}`, eventId: selected.id, eventName: selected.name, college: college.trim(), participants, createdAt: new Date().toISOString() }; const next = [...registrations, entry]; localStorage.setItem('avishkar-registrations', JSON.stringify(next)); setRegistrations(next); setRegistration(entry); setScreen('success'); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const copyId = async () => { if (!registration) return; await navigator.clipboard?.writeText(registration.id); setCopied(true); setTimeout(() => setCopied(false), 1800) }
  const downloadCsv = () => { const rows = [['Registration ID', 'Event', 'College', 'Participant', 'Mobile', 'Standard'], ...registrations.flatMap(r => r.participants.map(p => [r.id, r.eventName, r.college, p.name, p.mobile, p.standard]))]; const blob = new Blob([rows.map(row => row.map(cell => `"${cell.replaceAll('"', '""')}"`).join(',')).join('\n')], { type: 'text/csv' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'avishkar-registrations.csv'; a.click(); URL.revokeObjectURL(url) }

  return <main className="site-shell">
    <header className="site-header"><a href="#home" className="wordmark" onClick={() => { setScreen('home'); setMenuOpen(false) }}><span>AVISHKAR</span><b>5.0</b></a><button className="menu-button" aria-label="Open menu" onClick={() => setMenuOpen(!menuOpen)}>{menuOpen ? <X /> : <Menu />}</button>{menuOpen && <nav className="mobile-menu"><a href="#events" onClick={() => setMenuOpen(false)}>Events</a><button onClick={() => { setMenuOpen(false); setScreen('admin') }}>Organizer / Login</button><a href="#about" onClick={() => setMenuOpen(false)}>About</a></nav>}</header>
    {screen === 'home' && <>
      <section className="hero" id="home"><div className="hero-copy"><p className="eyebrow">KLE BCA COLLEGE, NIPANI</p><h1>AVISHKAR <em>5.0</em></h1><p className="hero-tagline">Where talent meets competition.</p><p className="hero-description">An inter-college fest for PUC I &amp; PUC II students ready to show what they&apos;ve got.</p><div className="hero-meta"><span>DATE — TO BE ANNOUNCED</span><span>KLE BCA COLLEGE, NIPANI</span></div><div className="hero-actions"><button className="button button-primary" onClick={() => document.getElementById('events')?.scrollIntoView({ behavior: 'smooth' })}>Explore events <ArrowRight /></button><button className="button button-secondary" onClick={() => openRegister(EVENTS[0])}>Register now</button></div></div><HeroArt /><div className="scroll-cue"><ArrowDown /> Scroll to discover</div></section>
      <section className="highlights" aria-label="Festival highlights">{[['FREE', 'REGISTRATION'], ['CASH', 'PRIZES'], ['MULTIPLE', 'EVENTS'], ['PUC I', '& PUC II']].map(([a, b], i) => <div className="highlight" key={b}><strong>{a}</strong><span>{b}</span></div>)}</section>
      <section className="events-section" id="events"><div className="section-heading"><p className="eyebrow">THE LINEUP</p><h2>Choose your <em>challenge.</em></h2><p>Pick an event, see the details, and register.</p></div>{grouped.map(({ group, events }) => <div className="event-group" key={group}><div className="group-title"><span>{group}</span><i>{String(events.length).padStart(2, '0')} EVENTS</i></div><div className="event-grid">{events.map(event => { const Icon = event.icon; return <button className={`event-card accent-${event.accent}`} key={event.id} onClick={() => setSelected(event)}><div className="event-visual"><Icon /><span className="card-number">{String(EVENTS.indexOf(event) + 1).padStart(2, '0')}</span></div><div className="event-info"><span>{event.group}</span><h3>{event.name}</h3><small><Users /> {teamLabel(event.teamSize)}</small></div><ArrowRight className="card-arrow" /></button> })}</div></div>)}</section>
      <section className="about-section" id="about"><div><p className="eyebrow">THE AVISHKAR SPIRIT</p><h2>Bring your <em>boldest</em> self.</h2></div><p>One campus. Fifteen ways to stand out. AVISHKAR 5.0 is where ideas get loud, teams get competitive, and every participant gets their moment.</p></section>
      <footer><div className="wordmark"><span>AVISHKAR</span><b>5.0</b></div><p>KLE BCA COLLEGE, NIPANI</p><span>Registration information will be announced soon.</span></footer>
    </>}
    {screen === 'register' && selected && <RegistrationFlow event={selected} step={step} setStep={setStep} college={college} setCollege={setCollege} participants={participants} updateParticipant={updateParticipant} addParticipant={addParticipant} removeParticipant={removeParticipant} errors={errors} submit={submit} back={() => setScreen('home')} />}
    {screen === 'success' && registration && <SuccessScreen registration={registration} copied={copied} copyId={copyId} again={() => openRegister(EVENTS.find(e => e.id === registration.eventId) || EVENTS[0])} back={() => setScreen('home')} />}
    {screen === 'admin' && <AdminPanel registrations={registrations} refresh={() => setRegistrations(JSON.parse(localStorage.getItem('avishkar-registrations') || '[]'))} remove={(id) => { const next = registrations.filter(r => r.id !== id); localStorage.setItem('avishkar-registrations', JSON.stringify(next)); setRegistrations(next) }} exportCsv={downloadCsv} back={() => setScreen('home')} />}
    {selected && screen === 'home' && <EventPreview event={selected} close={() => setSelected(null)} register={() => openRegister(selected)} />}
  </main>
}

function EventPreview({ event, close, register }: { event: Event; close: () => void; register: () => void }) { const Icon = event.icon; return <div className="overlay" role="presentation" onClick={close}><section className="sheet" role="dialog" aria-modal="true" aria-labelledby="preview-title" onClick={e => e.stopPropagation()}><button className="close-button" onClick={close} aria-label="Close"><X /></button><div className={`sheet-art accent-${event.accent}`}><Icon /><span>{event.group}</span></div><div className="sheet-content"><p className="eyebrow">{event.group}</p><h2 id="preview-title">{event.name}</h2><div className="team-pill"><Users /> {teamLabel(event.teamSize)}</div><p>{event.description}</p><h4>Prize</h4><p className="muted">{event.prize}</p><h4>Rules</h4><ul>{event.rules.map(rule => <li key={rule}>{rule}</li>)}</ul><button className="button button-primary wide" onClick={register}>Register now <ArrowRight /></button></div></section></div> }

function Progress({ step }: { step: number }) { return <div className="progress">{['College details', 'Participants', 'Review'].map((label, i) => <div className={step >= i + 1 ? 'active' : ''} key={label}><span>{i + 1}</span>{label}</div>)}</div> }
function RegistrationFlow({ event, step, setStep, college, setCollege, participants, updateParticipant, addParticipant, removeParticipant, errors, submit, back }: any) { return <section className="registration-page"><button className="back-link" onClick={back}><ArrowLeft /> Back to events</button><div className="registration-heading"><p className="eyebrow">REGISTRATION / {event.group.toUpperCase()}</p><h1>Join <em>{event.name}</em></h1><p>{teamLabel(event.teamSize)} · Registration is free.</p></div><Progress step={step} />{errors.length > 0 && <div className="error-box">{errors.map((error: string) => <span key={error}>{error}</span>)}</div>}{step === 1 && <div className="form-card"><h2>College details</h2><p>Tell us where your team is from.</p><label>College name<input value={college} onChange={e => setCollege(e.target.value)} placeholder="Enter your college name" /></label><button className="button button-primary wide" onClick={() => { if (college.trim()) setStep(2); else submit() }}>Continue <ArrowRight /></button></div>}{step === 2 && <div className="form-card"><div className="form-card-title"><div><h2>Participants</h2><p>Add everyone competing in this event.</p></div>{typeof event.teamSize !== 'number' && <div className="stepper"><button onClick={removeParticipant} disabled={participants.length <= event.teamSize.min}>−</button><strong>{participants.length}</strong><button onClick={addParticipant} disabled={participants.length >= event.teamSize.max}>+</button></div>}</div>{participants.map((p: Participant, i: number) => <div className="participant-block" key={i}><h3>Participant {i + 1}</h3><label>Full name<input value={p.name} onChange={e => updateParticipant(i, 'name', e.target.value)} placeholder="Enter full name" /></label><label>Mobile number<input inputMode="numeric" maxLength={10} value={p.mobile} onChange={e => updateParticipant(i, 'mobile', e.target.value.replace(/\D/g, ''))} placeholder="10-digit mobile number" /></label><label>Standard<select value={p.standard} onChange={e => updateParticipant(i, 'standard', e.target.value)}><option>PUC I</option><option>PUC II</option></select></label></div>)}<div className="form-actions"><button className="button button-secondary" onClick={() => setStep(1)}>Back</button><button className="button button-primary" onClick={() => { if (participants.every((p: Participant) => p.name.trim() && /^\d{10}$/.test(p.mobile))) setStep(3); else submit() }}>Review <ArrowRight /></button></div></div>}{step === 3 && <div className="form-card review-card"><h2>Review your registration</h2><p>Check the details before you submit.</p><div className="review-row"><span>Event</span><strong>{event.name}</strong></div><div className="review-row"><span>College</span><strong>{college}</strong></div><div className="review-participants"><span>Participants</span>{participants.map((p: Participant, i: number) => <div key={i}><b>{i + 1}. {p.name}</b><small>{p.standard} · {p.mobile}</small></div>)}</div><div className="form-actions"><button className="button button-secondary" onClick={() => setStep(2)}>Edit details</button><button className="button button-primary" onClick={submit}>Submit registration <Check /></button></div></div>}</section> }
function SuccessScreen({ registration, copied, copyId, again, back }: any) { return <section className="success-page"><div className="success-orbit" aria-hidden="true" /><div className="success-mark"><Check /></div><p className="eyebrow">REGISTRATION CONFIRMED</p><h1>You&apos;re <em>in.</em></h1><p className="success-lede">Bring your best. We&apos;ll see you at AVISHKAR 5.0.</p><div className="ticket"><span>EVENT</span><strong>{registration.eventName}</strong><span>REGISTRATION ID</span><b>{registration.id}</b><button onClick={copyId}>{copied ? <Check /> : <Clipboard />} {copied ? 'Copied' : 'Save registration ID'}</button></div><div className="success-actions"><button className="button button-primary" onClick={again}>Register another event <ArrowRight /></button><button className="button button-secondary" onClick={back}>Back to events</button></div></section> }
function AdminPanel({ registrations, refresh, remove, exportCsv, back }: any) { const [query, setQuery] = useState(''); const filtered = registrations.filter((r: Registration) => `${r.eventName} ${r.college}`.toLowerCase().includes(query.toLowerCase())); return <section className="admin-page"><button className="back-link" onClick={back}><ArrowLeft /> Student site</button><div className="admin-heading"><p className="eyebrow">ORGANIZER AREA · PROTOTYPE</p><h1>Registration <em>desk.</em></h1><p>Local preview data only. Connect a backend before launch.</p></div><div className="admin-toolbar"><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search event or college" /><button onClick={refresh}><RefreshCw /> Refresh</button><button onClick={exportCsv} disabled={!registrations.length}><Download /> Export CSV</button></div><div className="admin-stats"><strong>{registrations.length}</strong><span>registrations</span><strong>{registrations.reduce((sum: number, r: Registration) => sum + r.participants.length, 0)}</strong><span>participants</span></div>{filtered.length === 0 ? <div className="empty-state"><Trophy /><h2>No registrations yet</h2><p>Entries will appear here after a student submits the form.</p></div> : <div className="registration-list">{filtered.map((r: Registration) => <article key={r.id}><div className="registration-list-top"><div><span>{r.id}</span><h3>{r.eventName}</h3><p>{r.college}</p></div><button onClick={() => remove(r.id)} aria-label={`Delete ${r.id}`}><X /></button></div><div className="admin-participants">{r.participants.map((p: Participant, i: number) => <div key={i}><b>{p.name}</b><span>{p.standard} · {p.mobile}</span></div>)}</div></article>)}</div>}</section> }
